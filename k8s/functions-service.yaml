apiVersion: v1
kind: Service
metadata:
  name: contactkeeper-functions
  namespace: contactkeeper
spec:
  selector:
    app: contactkeeper-functions
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP